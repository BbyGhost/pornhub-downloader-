document.getElementById('scan').addEventListener('click',async()=>{const s=document.getElementById('status');s.textContent='Scanning…';const [tab]=await chrome.tabs.query({active:true,currentWindow:true});if(!tab?.id){s.textContent='No active tab.';return}try{await chrome.tabs.sendMessage(tab.id,{type:'SCAN_NOW'}).catch(()=>{});s.textContent='Scanner refreshed on the current page.'}catch(e){s.textContent=e.message}});
